fastapi>=0.110
httpx>=0.27
pydantic>=2.7
pyyaml>=6.0
rich>=13.7,<14
typer>=0.12
uvicorn>=0.29

# Development
pytest>=8.2

# Optional AgentFlow tool packages. These were not available on the active
# package index during setup, so keep them commented until the package source
# or private index is configured.
# agentflow-pii-detector
# agentflow-sql-analyzer
# agentflow-report-generator
